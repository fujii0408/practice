<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jp.co.illmatics.apps.shopping.repository.AdminUserMapper">

<select id="findAdminLogin" resultType="jp.co.illmatics.apps.shopping.model.AdminUser">
select email,
password
from admin_users
where email = #{email}
</select>

<select id="findAdminUsers" resultType="jp.co.illmatics.apps.shopping.model.AdminUser">
select id, name, email, is_owner
from admin_users
</select>

<select id="searchAdminUsers" resultType="jp.co.illmatics.apps.shopping.model.AdminUser">
select id, name, email, is_owner
from admin_users
where name like '%#{searchName}%'
or email like '%#{searchEmail}%'
</select>

<update id="updateAdminUser">
update admin_users
set name = #{name}, 
email = #{email}, 
password = #{password}, 
is_owner = #{is_owner}
where id = #{id} 
</update>

</mapper>